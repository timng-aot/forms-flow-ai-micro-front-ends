---
phase: 03-build-pipeline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - forms-flow-theme/package.json
  - tokens/scripts/precompute-colors.js
  - tokens/core.json
autonomous: true

must_haves:
  truths:
    - "All 24 blend-with-white-to-hex() expressions in core.json are resolved to clean hex values"
    - "style-dictionary and @tokens-studio/sd-transforms are installed as devDependencies"
    - "Pre-computation script is standalone and rerunnable without side effects on already-resolved values"
  artifacts:
    - path: "tokens/scripts/precompute-colors.js"
      provides: "Standalone color blending resolver"
      contains: "blendWithWhiteToHex"
    - path: "forms-flow-theme/package.json"
      provides: "Style Dictionary devDependencies"
      contains: "style-dictionary"
    - path: "tokens/core.json"
      provides: "Token file with resolved hex values (no blend expressions)"
  key_links:
    - from: "tokens/scripts/precompute-colors.js"
      to: "tokens/core.json"
      via: "reads JSON, resolves blend expressions, writes back"
      pattern: "readFileSync.*core\\.json"
---

<objective>
Install Style Dictionary tooling and create the pre-computation script that resolves blend-with-white-to-hex() SCSS expressions in core.json to clean hex values before Style Dictionary runs.

Purpose: Style Dictionary and Token Studio cannot parse SCSS function expressions. The 24 blend-with-white-to-hex() values in core.json must be resolved to hex values before any transformation or Figma import.

Output: forms-flow-theme devDependencies updated, tokens/scripts/precompute-colors.js created and validated, core.json contains only clean hex values.
</objective>

<execution_context>
@/Users/tng/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tng/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-build-pipeline/03-CONTEXT.md
@.planning/phases/03-build-pipeline/03-RESEARCH.md
@tokens/core.json
@forms-flow-theme/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies and create pre-computation script</name>
  <files>
    forms-flow-theme/package.json
    tokens/scripts/precompute-colors.js
  </files>
  <action>
    1. Install Style Dictionary and Token Studio transforms in forms-flow-theme:
       ```bash
       cd forms-flow-theme && npm install --save-dev style-dictionary @tokens-studio/sd-transforms
       ```

    2. Create tokens/scripts/precompute-colors.js (standalone Node.js script, no external dependencies):

       The script must:
       - Read tokens/core.json (path relative to repo root, resolved via __dirname)
       - Recursively walk all token objects looking for $value properties
       - Match pattern: blend-with-white-to-hex(#XXXXXX, N.N)
       - Resolve using formula: newRGB = (baseRGB * opacity) + (255 * (1 - opacity))
       - Write resolved JSON back to core.json with 2-space indentation + trailing newline
       - Be idempotent: running on already-resolved hex values produces no changes
       - Log count of resolved expressions and confirm completion

       Use the blendWithWhiteToHex() and resolveBlendExpressions() implementations from 03-RESEARCH.md Pattern 1.

       Important: The script reads from and writes to the same file (core.json). This is per user decision -- clean hex values only in token files, no preservation of original expressions (audit artifacts document originals).
  </action>
  <verify>
    - `ls tokens/scripts/precompute-colors.js` exists
    - `node -c tokens/scripts/precompute-colors.js` syntax check passes
    - `grep "style-dictionary" forms-flow-theme/package.json` shows devDependency
    - `grep "@tokens-studio/sd-transforms" forms-flow-theme/package.json` shows devDependency
  </verify>
  <done>
    style-dictionary and @tokens-studio/sd-transforms are in forms-flow-theme/package.json devDependencies. precompute-colors.js exists and passes syntax check.
  </done>
</task>

<task type="auto">
  <name>Task 2: Run pre-computation and verify resolved core.json</name>
  <files>
    tokens/core.json
  </files>
  <action>
    1. Run the pre-computation script:
       ```bash
       node tokens/scripts/precompute-colors.js
       ```

    2. Verify all 24 blend expressions are resolved:
       ```bash
       grep -c "blend-with-white-to-hex" tokens/core.json
       ```
       Must return 0 (no remaining blend expressions).

    3. Spot-check known values against the blending formula:
       - blend-with-white-to-hex(#3248F4, 1.0) should resolve to #3248f4 (100% opacity = original color)
       - blend-with-white-to-hex(#3248F4, 0.5) should resolve to #99a4fa (50% blend with white)
       - blend-with-white-to-hex(#3248F4, 0.25) should resolve to #ccd1fc (25% blend with white)

    4. Verify idempotency: run the script again and confirm core.json is unchanged (no diff).

    5. Verify the rest of core.json is intact: run the existing Python validation:
       ```bash
       python3 scripts/validate-tokens.py
       ```
       Note: Validation should still pass (blend expressions were previously accepted as strings; now they're valid hex colors which is even better).

    6. If Python validation needs adjustment for the resolved values (it might expect blend expressions), that's acceptable -- the hex values are the correct final form. The important check is that the JSON structure is intact and all other tokens are unchanged.
  </action>
  <verify>
    - `grep "blend-with-white-to-hex" tokens/core.json | wc -l` returns 0
    - `node -e "JSON.parse(require('fs').readFileSync('tokens/core.json','utf8'))"` valid JSON
    - Running precompute-colors.js a second time produces no file changes (idempotent)
    - core.json still has 160 core tokens with proper DTCG structure
  </verify>
  <done>
    core.json contains zero blend-with-white-to-hex() expressions. All 24 values are clean hex colors. Script is idempotent. JSON structure intact with all 160 core tokens preserved.
  </done>
</task>

</tasks>

<verification>
1. `ls forms-flow-theme/node_modules/style-dictionary` -- dependency installed
2. `ls forms-flow-theme/node_modules/@tokens-studio/sd-transforms` -- dependency installed
3. `node -c tokens/scripts/precompute-colors.js` -- script is valid JavaScript
4. `grep -c "blend-with-white-to-hex" tokens/core.json` returns 0
5. `node -e "const t = JSON.parse(require('fs').readFileSync('tokens/core.json','utf8')); console.log('Valid JSON')"` -- JSON intact
6. Running `node tokens/scripts/precompute-colors.js` twice produces identical output (idempotent)
</verification>

<success_criteria>
- style-dictionary v4+ and @tokens-studio/sd-transforms installed in forms-flow-theme/package.json
- tokens/scripts/precompute-colors.js exists, is syntactically valid, and resolves blend expressions
- tokens/core.json has zero blend-with-white-to-hex() expressions (all 24 resolved to hex)
- Pre-computation is idempotent (rerunning produces no changes)
- All 160 core tokens remain intact with valid DTCG structure
</success_criteria>

<output>
After completion, create `.planning/phases/03-build-pipeline/03-01-SUMMARY.md`
</output>
